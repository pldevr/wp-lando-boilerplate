name: wp-app-boilerplate
recipe: wordpress
config:
  php: '7.4'
  webroot: web/web
services:
  appserver:
    run:
      - sh /app/build/build.sh
  node:
    type: node:14
    run:
      - sh /app/build/build_node.sh
  mailhog:
    type: mailhog
    hogfrom:
      - appserver